package exceptions;

/**
 * Handles if the artist whose input name does not exist
 */
public class ArtistNotFoundException extends Exception {

    /**
     * Handles if the artist whose input name does not exist
     */
    public ArtistNotFoundException() {super();}
}
