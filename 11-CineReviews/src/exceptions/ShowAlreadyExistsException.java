package exceptions;

/**
 * Handles if the show's title already exists in the application either as a movie or series title
 */
public class ShowAlreadyExistsException extends Exception {

    /**
     * Handles if the show's title already exists in the application either as a movie or series title
     */
    public ShowAlreadyExistsException() {super();}
}
