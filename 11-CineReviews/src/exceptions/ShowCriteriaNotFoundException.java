package exceptions;

/**
 * Handles if no show was found with all the genres received as input || release date
 */
public class ShowCriteriaNotFoundException extends Exception {

    /**
     * Handles if no show was found with all the genres received as input || release date
     */
    public ShowCriteriaNotFoundException () {super();}
}
