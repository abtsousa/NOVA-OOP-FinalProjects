package exceptions;

/**
 * Handles if all artists have common projects || there's only one artist registered
 */
public class EmptyAvoidersException extends Exception {

    /**
     * Handles if all artists have common projects || there's only one artist registered
     */
    public EmptyAvoidersException() {super();}
}
